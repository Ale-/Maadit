{#
Template to process the about to expire mail that recurring payments module will send.
Available variables:
      - 'order_items': an array with the expiring items in order.
      - 'user': the customer object.
      - 'order_entity': the order entity object.
      - 'remaining days': the remaining days to expire, signed.
      - 'renew_url': the url to renew the purchase.
#}
{{ 'Hello @user'|t({ '@user': user.label }) }},
<br/>
{{ 'The following services are going to expire in @days days:'|t({ '@days' : remaining_days }) }}
<br/>
{{ order_items|length }}
{% for order_item in order_items %}
 <span>{{ order_item.label }}</span>
{% endfor %}
{{ dump(order_item) }}
<br />
{{ 'You can renew your services here:  @renew_url'|t({ '@renew_url' : renew_url }) }}.

<div class style="color:#ccc; font-size:smaill; margin-top:40px;">
<a href="{{ url('<front>') }}" style="text-decoration: none; font-weight: bold; margin-top: 15px;color:#555">{{ Maadix.net }}</a><br />
----
<h4>MaadixZone S.L.</h4>
<p>Carrer Roser 13<br />
08004 Barcelona<br />
NIF: B66869249</p>
</div>

